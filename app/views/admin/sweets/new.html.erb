商品新規登録

<div class="form-horizontal" role="form">
<%= form_for(@sweet, url: admin_sweets_path) do |f| %>
  <div class="form-group">
	<label>商品名</label>
	<p><%= f.text_field :name, class:"form-control" %></p>
  </div>
  <div class="form-group">
	<label>商品説明</label>
	<p><%= f.text_area :introduction, class:"form-control" %></p>
  </div>
  <p>ジャンル</p>
  <div class="form-group">
	<label>税抜き価格</label>
	<p><%= f.text_field :price, class:"form-control" %></p>
  </div>円
  <div class="form-group">
  	<label>販売ステータス</label>
  	<%= f.select :is_soldout, {'販売中':1,'売り切れ':2},{class: 'form-control'} %>
  </div>
  <div class="actions">
    <%= f.submit '新規登録', class: 'btn btn-primary btn-sm btn-block' %>
  </div>


<% end %>