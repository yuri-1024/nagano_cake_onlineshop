商品編集

<div class="form-horizontal" role="form">
	<%= form_for(@sweet, url: admin_sweet_path(@sweet.id)) do |f| %>
	  
	  <div class="form-group">
		<label>商品名</label>
		<p><%= f.text_field :name, class:"form-control" %></p>
	  </div>
	  <div class="form-group">
		<label>商品説明</label>
		<p><%= f.text_area :introduction, class:"form-control" %></p>
	  </div>
	  <div class="form-group">
	    <%= f.label :ジャンル %>
	    <%= f.collection_select :genre_id, Genre.all, :id, :name %>
	  </div>
	  <div class="form-group">
		<label>税抜き価格</label>
		<p><%= f.text_field :price, class:"form-control" %></p>
	  </div>円
	  <div class="form-group">
	  	<label>販売ステータス</label>
	  	<%= f.select :is_soldout, {'販売中':1,'売り切れ':2},{class: 'form-control'} %>
	  </div>
	  <div class="actions">
	    <%= f.submit '変更を保存', class: 'btn btn-primary btn-sm' %>
	  </div>


	<% end %>