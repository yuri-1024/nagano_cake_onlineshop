<!-- admin_top_index -->
<h4>管理者画面</h4>

<h3>注文件数
	<% order_amount = 0 %>
	<% @orders.each do |order| %>
		<% order.order_lists.each do |order_list| %>
		<% order_amount += order_list.amount %>
		<% end %>
		<%= link_to(admin_orders_path(order_sort: "0")) do %>
		<%= order_amount %>
		<% end %>
	<% end %>
	件
</h3>